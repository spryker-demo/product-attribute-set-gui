{% extends '@Gui/Layout/layout.twig' %}

{% block section_title 'Product Attribute Set Overview' | trans %}

{% block action %}
    {{ backActionButton('/product-attribute-set-gui', 'product_attribute_set_gui.product_attribute_set_list' | trans ) }}
{% endblock %}

{% block content %}
    {% embed '@Gui/Partials/widget.twig' with { widget_title: 'attribute-sets #' ~ productAttributeSet.idProductAttributeSet | trans } %}

        {% block widget_content %}

            <div class="row">
                <table class="table">
                    <thead></thead>
                    <tbody>
                        <tr>
                            <th class="col-lg-2">
                                {{ 'product_attribute_set_gui.name' | trans }}</th>
                            <td>{{ productAttributeSet.name }}</td>
                        </tr>
                        <tr>
                            <th>{{ 'product_attribute_set_gui.attributes' | trans }}</th>
                            <td>{{ productManagementAttributesNames | join(', ') }}</td>
                        </tr>
                    </tbody>
                    <tfoot></tfoot>
                </table>
            </div>

        {% endblock %}
    {% endembed %}
{% endblock %}

{% block footer_js %}
    {{ parent() }}
{% endblock %}
